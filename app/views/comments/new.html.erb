<h1>New Comment</h1>

<% frame_id = "new_#{dom_id(@commentable)}_comment" %>

<%= turbo_frame_tag frame_id do %>
  <%= render 'form', comment: @comment %>
  <%= link_to 'Back', :back, 'data-turbo-frame': frame_id %>
<% end %>
